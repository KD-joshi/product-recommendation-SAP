apiVersion: v1
kind: Service
metadata:
  name: product-similarity-service
  labels:
    app: product-similarity
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: product-similarity
